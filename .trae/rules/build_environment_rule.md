# 构建与编译环境约定

- 默认使用 `Visual Studio 2022`（VS17，MSVC `v143`）进行本地编译。
- 所有构建脚本与示例命令优先采用 `VS2022` 与 `x64` 平台。
- 使用 CMake 时，推荐生成器为 `Visual Studio 17 2022`，`architecture` 为 `x64`，`toolset` 为 `v143`。
- 未显式说明编译器版本时，按上述默认执行；如需变更，请在仓库根目录更新对应配置文件。

