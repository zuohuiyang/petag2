# 测试报告（模板）

## 概述
- 目标：验证PE解析、签名有效性、元数据读写一致性。

## 环境
- Windows 10/11 x64，VS2022，BoringSSL crypto.lib。

## 用例
- 正常：`{test_chan:123}`、渠道号+版本组合。
- 边缘：空数据、超长数据(>30)、无效PE、未签名PE。

## 结果
- 插入成功且签名验证通过。
- 读取元数据与输入一致。
- 非法输入均被拒绝并给出明确错误信息。

